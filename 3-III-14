/*
#include <iostream>
using namespace std;
int main() {
    int a, b, i;
    cin >> a >> b;
    for (i = a; i <= b; i++) {
        if (i % 3 == 0) {
            cout << "Hui= " << i << endl;
        }
    }
}
*/
/*
#include <iostream>
using namespace std;
int main() {
    int a, b, i;
    cin >> a >> b;
    i = a;
    while (i <= b) {
        if (i % 3 == 0) {
            cout << "Opa =" << i << endl;
        }
        i++;
    }
}
*/
/*
#include <iostream>
using namespace std;
int main() {
    int a, b, i;
    cin >> a >> b;
    i = a;
    do {
        if (i % 3 == 0) {
            cout << "Kill me = " << i << endl; 
        }
        i++;
    } 
    while (i <= b); 
}
*/
